---
phase: 05-mece-so-what
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/idea-to-task/references/decision-rules.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-03
must_haves:
  truths:
    - "decision-rules.md 第 6 节存在且明确四维 MECE 校验：同层互斥 / 同层完备 / 数量 2-4 / 分组一致性"
    - "§6 内部推理使用 <mece_check> XML 标签，指令明确不得出现在用户可见输出"
    - "§6 要求每层同层分支显式标 grouping: inductive 或 grouping: deductive（QUAL-03 落地）"
    - "§6 underflow 规则兜底到 2 层结构并在风险段标注（D-06）"
    - "§6 overflow 规则优先触发归纳聚类，无法聚合时 top 3 + 其余降级（D-07）"
    - "弱证据场景自动退化为仅「同层 2-4 数量检查」（D-03）"
  artifacts:
    - path: "skills/idea-to-task/references/decision-rules.md"
      provides: "§6 MECE 互斥完备校验规则"
      contains: "## 6. MECE 互斥完备校验"
  key_links:
    - from: "decision-rules.md §6"
      to: "decision-rules.md §3 证据强度表"
      via: "弱证据退化为数量检查"
      pattern: "弱证据.*(退化|仅|只).*(数量|2-4)"
    - from: "decision-rules.md §6"
      to: "output-template.md §3 层级规则"
      via: "cardinality 2-4 字面一致"
      pattern: "2 到 4|2-4"
---

<objective>
在 `skills/idea-to-task/references/decision-rules.md` 末尾新增 §6「MECE 互斥完备校验」。本节定义 AI 在生成金字塔输出之前必须执行的四维内部校验规则（同层互斥 / 同层完备 / 数量 2-4 / 分组一致性），并覆盖 cardinality 的 underflow/overflow 兜底规则。

Purpose: 让 AI 在输出前能识别同层任务重叠、分组标准混杂、数量失控等结构问题，把「文字正确」升级为「结构正确」。规则对用户透明（推理过程放入 `<mece_check>` XML，最终输出不暴露术语）。

Output: decision-rules.md §6 完整章节，约 60-100 行新增内容。
</objective>

<execution_context>
@/Users/Bai/Downloads/同步空间/works/Code/idea-to-task/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Bai/Downloads/同步空间/works/Code/idea-to-task/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mece-so-what/05-CONTEXT.md
@.planning/phases/04-pyramid/04-REVIEWS.md
@skills/idea-to-task/references/decision-rules.md
@skills/idea-to-task/references/output-template.md
@skills/idea-to-task/SKILL.md
</context>

<interfaces>
<!-- 本 plan 新增 §6 的结构锚点 -->
decision-rules.md 当前章节：
- § 1. SCQA 推演 (L3)
- § 2. SCQA 追问触发 (L57)
- § 3. 证据强度与 SCQA 完整度 (L77)
- § 4. 高风险歧义 (L93)
- § 5. 常见基线偏差 (L101)
- 文件末尾 (L110) ← §6 追加插入点

output-template.md §3 层级规则字面约束（必须与 §6 对齐）：
- "同一层的支撑点数量保持在 2 到 4 个"
- "弱证据：展开到主题层（2 层结构）"

Phase 3 XML 隔离模式（必须沿用）：
```xml
<scqa_analysis>
S: ...
C: ...
Q: ...
A: ...
完整度: [强/中/弱]
</scqa_analysis>
```
§6 的 `<mece_check>` 采用同风格：内部推理、标签嵌入、不进最终输出。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 在 decision-rules.md 末尾追加 §6「MECE 互斥完备校验」骨架与四维规则</name>
  <files>skills/idea-to-task/references/decision-rules.md</files>
  <read_first>
    - skills/idea-to-task/references/decision-rules.md（确认现有 §1-§5 结构与文末位置）
    - skills/idea-to-task/references/output-template.md（§3 层级规则，cardinality 字面必须一致）
    - .planning/phases/05-mece-so-what/05-CONTEXT.md（D-01、D-02、D-03、D-04 的约束）
    - .planning/phases/03-scqa/03-CONTEXT.md（D-04/D-05 XML 推理隔离模式）
  </read_first>
  <action>
在 decision-rules.md 当前末尾（第 110 行后）追加 §6，结构如下（措辞可适度打磨，但必须保留所有列出的锚点字符串）：

```markdown
## 6. MECE 互斥完备校验

**触发时机**：金字塔输出生成前，在 SCQA 推演完成之后、任务树渲染之前执行一次。对用户透明，推理过程放入 `<mece_check>` 标签，不进入最终输出。

**四维校验**（QUAL-01 全覆盖）：

1. **同层互斥**：同一层的支撑点语义必须彼此排斥，不得出现"提升效率 vs 优化流程"此类表述重叠的并列项。发现重叠时，合并同类项或改用更具区分度的措辞。
2. **同层完备**：同一父节点下的支撑点必须覆盖父节点所要回答的全部关键维度；若覆盖不足，在「风险与高风险推断」段落显式标注缺口（例如「当前拆分未覆盖 X 维度，存在推进盲区」）。
3. **数量 2-4**：每一层的支撑点数量必须落在 2 到 4 之间（与 output-template.md §3、SKILL.md 工作流步骤 5 字面一致）；低于 2 或高于 4 触发第 7 条 underflow/overflow 规则。
4. **分组一致性**（QUAL-03 落地）：每层同层分支必须采用同一分组标准。`<mece_check>` 内对每层显式标记 `grouping: inductive | deductive`：
   - `grouping: inductive` — 分支是从多条观察事实归纳上升而来，校验时检查"分类标准是否一致"（如全部按业务线拆 / 全部按阶段拆，不得混用）。
   - `grouping: deductive` — 分支是从上层结论演绎下降而来，校验时检查"因果推导链是否完整"（子节点是否确实是父节点的必要支撑，不得跳链）。
   - 同层分支不得混用两种 grouping。

**内部推理格式**：

\`\`\`xml
<mece_check>
层级: [主题层 | 里程碑层 | 执行项层]
分支: [分支1, 分支2, ...]
grouping: [inductive | deductive]
互斥性: [通过 | 发现重叠：...]
完备性: [通过 | 缺口：...]
数量: [N 个，状态：通过 / underflow / overflow]
分组一致性: [通过 | 不一致：...]
处置: [通过直出 | 合并 | 补充 | 触发 §6 cardinality 规则]
</mece_check>
\`\`\`

**弱证据退化**（D-03）：

当 §3 判定证据强度为「弱」且输出只到主题层（2 层结构）时，`<mece_check>` 自动退化为仅执行「数量 2-4」单维检查；不再要求互斥性 / 完备性 / 分组一致性维度，避免对无分层的输出过度校验。

**输出隔离纪律**：

- `<mece_check>` 标签及其内部字段（`grouping`、`inductive`、`deductive`、`互斥性`、`完备性` 等）绝对不得出现在用户可见输出中。
- 用户只看到结构正确的金字塔任务树，看不到校验过程。
- 术语泄漏禁令详见 SKILL.md「明确禁止」段。
```

说明：
- `<mece_check>` 标签嵌套结构遵循 Phase 3 的 `<scqa_analysis>` 风格（内部推理、XML 隔离、不进最终输出）
- "2 到 4" 字面必须与 output-template.md §3、SKILL.md 工作流步骤 5 完全一致（层级规则三处字面纪律）
- D-09 决策明确「不做证据强度量化」，本节不引入"3+ 业务维度"等量化准则
  </action>
  <verify>
    <automated>test $(grep -c "^## 6\\. MECE 互斥完备校验" skills/idea-to-task/references/decision-rules.md) -eq 1 && grep -q "同层互斥" skills/idea-to-task/references/decision-rules.md && grep -q "同层完备" skills/idea-to-task/references/decision-rules.md && grep -q "数量 2-4\\|数量.*2 到 4" skills/idea-to-task/references/decision-rules.md && grep -q "分组一致性" skills/idea-to-task/references/decision-rules.md && grep -q "grouping: inductive" skills/idea-to-task/references/decision-rules.md && grep -q "grouping: deductive" skills/idea-to-task/references/decision-rules.md && grep -q "<mece_check>" skills/idea-to-task/references/decision-rules.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## 6\\. MECE 互斥完备校验" decision-rules.md` 等于 1
    - 文件包含字符串「同层互斥」、「同层完备」、「分组一致性」（四维全覆盖）
    - 文件包含字符串 `<mece_check>` 以及 `grouping: inductive` 和 `grouping: deductive`（QUAL-03 显式标记）
    - 文件包含字符串「2 到 4」或「2-4」（与 output-template.md §3 字面一致）
    - 文件包含字符串「弱证据」同句中出现「退化」或「仅」或「只」且出现「数量」（弱证据退化规则落地）
    - 不得出现「3 个业务维度」「单句口号」等证据强度量化措辞（D-09 纪律）
    - §6 位于文件末尾，§1-§5 的章节标题行号与改动前完全一致（`grep -n "^## [1-5]\\." decision-rules.md` 前 5 行不变）
  </acceptance_criteria>
  <done>§6 首节（四维校验 + 内部推理格式 + 弱证据退化 + 输出隔离）完整落地并通过全部 grep 断言</done>
</task>

<task type="auto">
  <name>Task 2: 在 §6 末尾追加 cardinality underflow / overflow 兜底规则</name>
  <files>skills/idea-to-task/references/decision-rules.md</files>
  <read_first>
    - skills/idea-to-task/references/decision-rules.md（Task 1 写入的 §6 现状）
    - .planning/phases/05-mece-so-what/05-CONTEXT.md（D-06、D-07 兜底规则约束）
    - .planning/phases/04-pyramid/04-REVIEWS.md（Codex MEDIUM 关切：cardinality underflow/overflow 规则缺失）
    - skills/idea-to-task/references/decision-rules.md §1 第 4 步（向上归纳逻辑，overflow 聚类复用）
    - skills/idea-to-task/references/decision-rules.md §2（SCQA 追问机制，underflow 优先由它捕获）
  </read_first>
  <action>
在 §6 已有内容末尾（输出隔离纪律之后）追加 cardinality 兜底规则子节，格式：

```markdown
**Cardinality 兜底规则**：

当「数量 2-4」维度检查命中 underflow 或 overflow 时，按以下规则处置，`<mece_check>` 不越权接管补问或重构金字塔。

- **Underflow（同层 <2 个稳定候选）**：
  1. 视为「输入过窄」，优先由 §2 SCQA 追问机制在上游捕获；若追问已跳过或无法追加，则进入兜底。
  2. 兜底退化为 2 层结构（核心判断 → 里程碑），即取消主题层，直接以里程碑承接核心判断。
  3. 在「风险与高风险推断」段落显式标注：「输入不足以支撑多主题拆分，已退化为单主题结构」。
  4. `<mece_check>` 仅输出 `处置: underflow 兜底 → 2 层结构`，不尝试拼凑第 2 个主题。

- **Overflow（同层 >4 个候选）**：
  1. 优先触发归纳聚类，复用 §1 第 4 步已定义的归纳收敛能力，尝试将相近候选合并为 2-4 个主题。
  2. 若无法自然合并（候选语义互斥、共同上位概念不成立），选取与核心判断距离最近的 top 3 作为主题，其余候选降级处理：
     - 符合里程碑粒度的，归入某个保留主题下作为里程碑。
     - 仍保持独立方向但证据不足的，写入「需补问」清单或「风险与高风险推断」段落。
  3. `<mece_check>` 输出 `处置: overflow 聚类 → N 个主题 + M 个降级项` 或 `处置: overflow top3 + 补问清单`。

- **纪律约束**：
  - underflow 情况下 MECE 不得越权触发追问；追问权归 §2，§6 只做结构兜底。
  - overflow 聚类必须可逆向回溯（原候选在 `<mece_check>` 内留痕），不允许静默删除候选。
```

说明：
- 复用 §1 第 4 步的归纳逻辑，不重写
- 与 §2 SCQA 追问机制的职责边界清晰（追问归 §2，结构兜底归 §6）
  </action>
  <verify>
    <automated>grep -q "Cardinality 兜底规则\\|cardinality 兜底\\|underflow\\|overflow" skills/idea-to-task/references/decision-rules.md && grep -q "underflow" skills/idea-to-task/references/decision-rules.md && grep -q "overflow" skills/idea-to-task/references/decision-rules.md && grep -q "2 层结构" skills/idea-to-task/references/decision-rules.md && grep -q "归纳聚类\\|归纳收敛\\|聚类" skills/idea-to-task/references/decision-rules.md && grep -q "top 3\\|top3" skills/idea-to-task/references/decision-rules.md</automated>
  </verify>
  <acceptance_criteria>
    - 文件包含字符串 `underflow` 和 `overflow`（两种边界明确命名）
    - 文件包含字符串「2 层结构」（underflow 兜底目标）
    - 文件包含「归纳聚类」或「归纳收敛」或「聚类」（overflow 复用 §1 第 4 步）
    - 文件包含「top 3」或「top3」（overflow 降级策略）
    - 文件包含「追问权归 §2」或等价措辞（边界纪律）
    - §6 章节整体总行数新增 ≥40 行：`awk '/^## 6/,/^## 7|^$/' decision-rules.md | wc -l` 返回 ≥ 40（粗略下限，只作 sanity check）
  </acceptance_criteria>
  <done>§6 末尾 cardinality 兜底子节落地，underflow / overflow 两条路径都给出具体处置步骤</done>
</task>

</tasks>

<verification>
- `grep -c "^## 6\\. MECE 互斥完备校验" skills/idea-to-task/references/decision-rules.md` 等于 1
- 四维校验锚点均可 grep 命中：同层互斥 / 同层完备 / 数量 2-4 / 分组一致性
- `<mece_check>` XML 块、`grouping: inductive`、`grouping: deductive` 三处字面锚点均存在
- underflow / overflow 两条兜底路径均存在且给出结构化处置
- §1-§5 章节标题与 Task 1 前完全一致（未误改上游内容）
- output-template.md 与 examples.md 未被修改：`git diff --name-only skills/idea-to-task/references/output-template.md skills/idea-to-task/references/examples.md` 为空
</verification>

<success_criteria>
- decision-rules.md §6 完整落地，覆盖 QUAL-01 全四维 + QUAL-03 分组标记
- D-01（XML 隔离）、D-02（四维）、D-03（弱证据退化）、D-04（grouping 标记）、D-06（underflow 兜底）、D-07（overflow 聚类+降级）全部以规则条文形式在文件中体现
- 未引入 D-09 排除的量化证据准则
- 文件改动严格限制在 decision-rules.md 单文件
</success_criteria>

<output>
完成后创建 `.planning/phases/05-mece-so-what/05-01-SUMMARY.md`
</output>
