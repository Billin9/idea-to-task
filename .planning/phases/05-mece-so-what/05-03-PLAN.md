---
phase: 05-mece-so-what
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - skills/idea-to-task/SKILL.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
must_haves:
  truths:
    - "SKILL.md 工作流在步骤 5（主题识别与组织）与步骤 6（输出）之间新增一步「内部质量校验」"
    - "新增步骤明确 <mece_check> + <sowhat_filter> 串行执行且对用户透明"
    - "SKILL.md「明确禁止」段扩展禁词，禁止用户输出出现 mece_check / sowhat_filter / 分组 / grouping / inductive / deductive"
    - "SKILL.md「何时读取参考文件」注册 §6 和 §7 的懒加载触发条件"
  artifacts:
    - path: "skills/idea-to-task/SKILL.md"
      provides: "工作流新步骤 + 扩展禁词 + 懒加载注册"
      contains: "内部质量校验"
  key_links:
    - from: "SKILL.md 新增步骤"
      to: "decision-rules.md §6 / §7"
      via: "详见链接引用"
      pattern: "decision-rules.md.*(第 6 节|第 7 节|§6|§7|6 节|7 节)"
    - from: "SKILL.md 明确禁止段"
      to: "decision-rules.md §6 / §7 内部标签"
      via: "禁词覆盖 mece_check/sowhat_filter/grouping/inductive/deductive/分组"
      pattern: "mece_check|sowhat_filter|grouping|inductive|deductive"
---

<objective>
在 `skills/idea-to-task/SKILL.md` 完成三处改动，把 Plan 01/02 新增的 §6/§7 规则接入主工作流：

1. 工作流段新增一步「内部质量校验」，插入在步骤 5（主题识别与组织）与步骤 6（输出核心判断...）之间
2. 「明确禁止」段扩展禁词表，覆盖 `mece_check / sowhat_filter / 分组 / grouping / inductive / deductive`
3. 「何时读取参考文件」段注册 §6（`<mece_check>` 启动时）和 §7（`<sowhat_filter>` 启动时）的懒加载触发条件

Purpose:
- D-10 落地：把内部推理步骤编码进 SKILL.md 的 Procedural CoT
- 堵死术语泄漏：扩展禁词是 D-10 明确要求的防线
- 保证懒加载：SKILL.md 是单一事实源，懒加载触发条件必须同步注册，Phase 5 才算闭环

Output: SKILL.md 三处改动，总行数预计增加 8-15 行。
</objective>

<execution_context>
@/Users/Bai/Downloads/同步空间/works/Code/idea-to-task/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Bai/Downloads/同步空间/works/Code/idea-to-task/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-mece-so-what/05-CONTEXT.md
@.planning/phases/05-mece-so-what/05-01-PLAN.md
@.planning/phases/05-mece-so-what/05-02-PLAN.md
@skills/idea-to-task/SKILL.md
@skills/idea-to-task/references/decision-rules.md
@skills/idea-to-task/references/output-template.md
</context>

<interfaces>
<!-- Plan 01/02 已写入 decision-rules.md 的锚点（本 plan 引用时字面必须一致） -->
- `## 6. MECE 互斥完备校验` — SKILL.md 引用为「decision-rules.md 第 6 节」
- `## 7. So-what 空洞过滤` — SKILL.md 引用为「decision-rules.md 第 7 节」
- `<mece_check>` / `<sowhat_filter>` — SKILL.md 在工作流和禁词段都要明确标注

<!-- SKILL.md 当前工作流步骤（L28-L38） -->
1. 识别输入类型（L30）
2. SCQA 推演（L31）
3. 评估 SCQA 完整度（L32）
4. 追问判断（L33）
5. 主题识别与组织（L34，本 plan 在此之后插入新步骤）
6. 输出核心判断、主题拆分、任务树...（L35，本 plan 将其后移为步骤 7 或保留 5.5 → 6 的衔接）

<!-- SKILL.md 当前「何时读取参考文件」段（L66-L70） -->
- decision-rules.md 加载条件：SCQA 推演 / 触发追问 / 确认证据强度（本 plan 新增 §6 §7 加载条件）
- output-template.md 加载条件
- examples.md 加载条件

<!-- SKILL.md 当前「明确禁止」段（L72-L81，共 9 条禁令） -->
- 本 plan 扩展第 7 条（「支柱」「金字塔」「pillar」「MECE」「So-what」）或新增一条，覆盖 mece_check / sowhat_filter / 分组 / grouping / inductive / deductive
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 在 SKILL.md 工作流段插入新步骤「内部质量校验」</name>
  <files>skills/idea-to-task/SKILL.md</files>
  <read_first>
    - skills/idea-to-task/SKILL.md（当前工作流段 L28-L41，确认步骤 5 与步骤 6 的字面位置）
    - skills/idea-to-task/references/decision-rules.md（Plan 01/02 写入的 §6 / §7 章节编号和标题）
    - .planning/phases/05-mece-so-what/05-CONTEXT.md（D-10 决策原文 + D-11 「规则单一事实源」纪律）
  </read_first>
  <action>
在 SKILL.md 工作流段修改步骤列表，将现有步骤 6 改写为「输出前先执行内部质量校验，再渲染最终输出」结构。推荐采用「插入为新步骤 6，原步骤 6 变为步骤 7」方案，改动后工作流为：

```markdown
## 工作流

1. 识别输入类型：长文本、碎片短句或混合输入
2. **SCQA 推演（内部）**：在 `<scqa_analysis>` 中从输入推演 S → C → Q → A，详见 [decision-rules.md](./references/decision-rules.md)（decision-rules.md 第 1 节）
3. 评估 SCQA 完整度：根据四项信息齐全程度确定证据强度（强 / 中 / 弱），详见 [decision-rules.md](./references/decision-rules.md)（decision-rules.md 第 3 节）
4. 追问判断：当 SCQA 关键要素缺失且影响方向时，触发 2 到 3 个定向追问；若信息足够则跳过，详见 [decision-rules.md](./references/decision-rules.md)（decision-rules.md 第 2 节）
5. 主题识别与组织：基于 SCQA 推演结果识别主题结构；同层主题数量保持在 2 到 4 个，保持内部组织规则对用户透明，详见 [output-template.md](./references/output-template.md) 第 3 节
6. **内部质量校验（内部）**：在输出渲染之前串行执行两块对用户透明的校验：
   - 先在 `<mece_check>` 中按四维（同层互斥 / 同层完备 / 数量 2-4 / 分组一致性）校验任务树，并处理 cardinality underflow / overflow 兜底，详见 [decision-rules.md](./references/decision-rules.md) 第 6 节
   - 再在 `<sowhat_filter>` 中按 5 类空洞模式（无主语口号 / 无对象动作 / 无验收里程碑 / 父子重复 / 无法行动描述）做字面扫描 + 语义自问双道过滤，详见 [decision-rules.md](./references/decision-rules.md) 第 7 节
   - 两块推理产生的内部标签、字段和分组标记绝对不得进入最终输出
7. 输出核心判断、主题拆分、任务树、执行建议、风险提醒、下一步动作
```

注意：
- 原「遇到边界不清的输入时」段落保留在步骤 7 之后，位置不变
- 不要在 SKILL.md 里复述 §6 / §7 的具体规则条文（D-11 规则单一事实源纪律）
- 字面「2 到 4」「5 类空洞模式」必须与 output-template.md §3、decision-rules.md §6/§7 一致
  </action>
  <verify>
    <automated>grep -q "内部质量校验" skills/idea-to-task/SKILL.md && grep -q "<mece_check>" skills/idea-to-task/SKILL.md && grep -q "<sowhat_filter>" skills/idea-to-task/SKILL.md && grep -q "decision-rules.md.*第 6 节\\|第 6 节" skills/idea-to-task/SKILL.md && grep -q "decision-rules.md.*第 7 节\\|第 7 节" skills/idea-to-task/SKILL.md && grep -q "^7\\. \\|^7\\." skills/idea-to-task/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - 文件包含字符串「内部质量校验」（新步骤名）
    - 文件包含 `<mece_check>` 和 `<sowhat_filter>` 两个 XML 标签字面
    - 文件包含「decision-rules.md 第 6 节」或「第 6 节」（§6 引用）
    - 文件包含「decision-rules.md 第 7 节」或「第 7 节」（§7 引用）
    - 工作流列表包含第 7 步：`grep -cE "^7\\." SKILL.md` ≥ 1
    - 文件包含「同层互斥」或「互斥」+「完备」（MECE 四维简述至少可 grep 到核心词）
    - 文件包含「5 类空洞」或「空洞模式」（So-what 简述锚点）
    - 不复写 §6/§7 的完整规则条文（`awk '/^6\\. \\*\\*内部质量校验/,/^7\\./' SKILL.md | wc -l` ≤ 10，粗略限制新步骤篇幅）
  </acceptance_criteria>
  <done>SKILL.md 工作流段插入新步骤 6「内部质量校验」，原步骤 6 后移为步骤 7</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 SKILL.md「明确禁止」段禁词 + 「何时读取参考文件」段懒加载注册</name>
  <files>skills/idea-to-task/SKILL.md</files>
  <read_first>
    - skills/idea-to-task/SKILL.md（Task 1 完成后的当前状态，特别是「何时读取参考文件」段 L66-L70 和「明确禁止」段 L72-L81）
    - skills/idea-to-task/references/decision-rules.md（Plan 01/02 的 §6 / §7 XML 标签和 grouping 字段）
    - .planning/phases/05-mece-so-what/05-CONTEXT.md（D-10 扩展禁词清单）
  </read_first>
  <action>
**改动 A — 「何时读取参考文件」段**：在现有 decision-rules.md 加载条件之后扩展，使其覆盖 §6 / §7 懒加载。推荐改写为：

```markdown
## 何时读取参考文件

- 当你需要执行 SCQA 推演、判断是否触发定向追问，或确认证据强度时，读取 [decision-rules.md](./references/decision-rules.md) 第 1-3 节
- 当你进入工作流步骤 6「内部质量校验」并启动 `<mece_check>` 时，读取 [decision-rules.md](./references/decision-rules.md) 第 6 节
- 当你在内部质量校验中启动 `<sowhat_filter>` 时，读取 [decision-rules.md](./references/decision-rules.md) 第 7 节
- 当你需要稳定输出格式时，读取 [output-template.md](./references/output-template.md)
- 当你需要校准风格或边界时，读取 [examples.md](./references/examples.md)
```

**改动 B — 「明确禁止」段**：在现有禁词段末尾新增两条，明确覆盖 `mece_check / sowhat_filter / 分组 / grouping / inductive / deductive`。推荐改写为追加两条：

```markdown
- 不在输出中使用或暴露 `<mece_check>`、`<sowhat_filter>` 等内部校验标签及其字段
- 不在用户可见输出中使用「分组」「grouping」「inductive」「deductive」等内部分组标记术语（属 QUAL-03 内部推理，对用户透明）
```

整段「明确禁止」最终应包含至少 10 条禁令（原 9 条 + 新增 2 条）。

注意：
- 禁词列表字面必须包含：`mece_check`、`sowhat_filter`、`grouping`、`inductive`、`deductive`、`分组`（共 6 个禁词）
- 原有 9 条禁令保持不动，只做追加
- 「何时读取参考文件」段原有 3 条改为 5 条（decision-rules.md 拆成 3 条：§1-3 / §6 / §7）
  </action>
  <verify>
    <automated>grep -q "mece_check" skills/idea-to-task/SKILL.md && grep -q "sowhat_filter" skills/idea-to-task/SKILL.md && grep -q "grouping" skills/idea-to-task/SKILL.md && grep -q "inductive" skills/idea-to-task/SKILL.md && grep -q "deductive" skills/idea-to-task/SKILL.md && grep -q "分组" skills/idea-to-task/SKILL.md && grep -q "第 6 节\\|6 节" skills/idea-to-task/SKILL.md && grep -q "第 7 节\\|7 节" skills/idea-to-task/SKILL.md && test $(awk '/^## 明确禁止/,/^$/{if($0~/^- /) print}' skills/idea-to-task/SKILL.md | wc -l) -ge 10</automated>
  </verify>
  <acceptance_criteria>
    - 「明确禁止」段禁词字面全部命中：`grep -c "mece_check\\|sowhat_filter\\|grouping\\|inductive\\|deductive" SKILL.md` ≥ 5（至少 5 类术语各命中 1 次）
    - 禁词段包含中文「分组」（与英文 `grouping` 配对覆盖中英文泄漏）
    - 「明确禁止」段禁令条目数 ≥ 10：`awk '/^## 明确禁止/,0' SKILL.md | grep -c "^- "` ≥ 10
    - 「何时读取参考文件」段至少 5 条：`awk '/^## 何时读取参考文件/,/^## /' SKILL.md | grep -c "^- "` ≥ 5
    - 「何时读取参考文件」段引用「第 6 节」和「第 7 节」各至少 1 次
    - 原 9 条禁令仍存在（例如「不跳过 SCQA 推演直接输出任务树」仍可 grep 命中）
  </acceptance_criteria>
  <done>SKILL.md「明确禁止」新增 2 条覆盖 6 个内部术语，「何时读取参考文件」新增 §6/§7 懒加载触发条件</done>
</task>

</tasks>

<verification>
- SKILL.md 工作流第 6 步为「内部质量校验」，第 7 步为原「输出核心判断...」
- `<mece_check>` 和 `<sowhat_filter>` 在工作流和「明确禁止」段都字面出现
- `mece_check / sowhat_filter / grouping / inductive / deductive / 分组` 6 个禁词全部可 grep 命中
- 「何时读取参考文件」段覆盖 §6/§7 两个懒加载条件
- decision-rules.md 未被修改（本 plan 只改 SKILL.md）：`git diff --name-only skills/idea-to-task/references/decision-rules.md` 为空
- output-template.md 与 examples.md 未被修改
- SKILL.md 的 「适用场景」「核心原则」「补问规则」「输出结构」四段未被误改：对应章节标题行仍可 grep 命中（`grep -c "^## 适用场景\\|^## 核心原则\\|^## 补问规则\\|^## 输出结构" SKILL.md` 等于 4）
</verification>

<success_criteria>
- SKILL.md 工作流、禁词表、懒加载注册三处改动完成
- D-10（SKILL.md 新增步骤 + 扩展禁词）完整落地
- Plan 01/02 写入的 §6 §7 通过 SKILL.md 的 Procedural CoT 接入主工作流
- 规则单一事实源纪律未破坏：SKILL.md 只引用 §6/§7 章节编号，不复写规则条文
</success_criteria>

<output>
完成后创建 `.planning/phases/05-mece-so-what/05-03-SUMMARY.md`
</output>
