---
phase: 01-judgment-output-refactor
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - skills/idea-to-task/SKILL.md
  - tests/fixtures/idea-to-task/04-induction-success.md
  - tests/fixtures/idea-to-task/05-induction-failure.md
autonomous: true
requirements: [JUDGE-01, JUDGE-02, OUT-01, OUT-02, OUT-03]

must_haves:
  truths:
    - "SKILL.md 工作流在拆主题和输出之间存在独立的「向上归纳检查」步骤"
    - "SKILL.md 核心原则从战略型专用扩展为多主题通用归纳原则"
    - "SKILL.md 工作流步骤引用了 decision-rules.md 正确的章节编号（第 3 节）"
    - "存在行为级验证 fixture 能证明归纳成功场景的期望输出"
    - "存在行为级验证 fixture 能证明归纳失败时静默回退的期望行为"
  artifacts:
    - path: "skills/idea-to-task/SKILL.md"
      provides: "更新后的工作流（含归纳步骤）和核心原则（含通用归纳原则）"
      contains: "向上归纳检查"
    - path: "tests/fixtures/idea-to-task/04-induction-success.md"
      provides: "归纳成功场景的输入和期望行为"
      contains: "归纳"
    - path: "tests/fixtures/idea-to-task/05-induction-failure.md"
      provides: "归纳失败场景的输入和期望行为"
      contains: "独立输出"
  key_links:
    - from: "skills/idea-to-task/SKILL.md"
      to: "skills/idea-to-task/references/decision-rules.md"
      via: "工作流步骤引用 decision-rules.md 第 1 节和第 3 节"
      pattern: "decision-rules.md.*第 [13] 节"
    - from: "skills/idea-to-task/SKILL.md"
      to: "skills/idea-to-task/references/output-template.md"
      via: "输出结构引用 output-template.md"
      pattern: "output-template.md"
---

<objective>
更新 SKILL.md 工作流和核心原则以整合向上归纳逻辑，并创建行为级验证 fixture 证明归纳功能确实改变了 skill 输出行为。

Purpose: 将 Plan 01 中新增的规则和模板接入 skill 入口文件，并通过行为 fixture 解决评审指出的「只有结构校验无法证明行为改变」问题。
Output: 更新后的 SKILL.md + 2 个新增行为 fixture（归纳成功/归纳失败）。

Review concerns addressed:
- [HIGH] 缺少行为级验证 — 新增 2 个 fixture：04-induction-success.md（证明多主题收敛为单顶层意图）和 05-induction-failure.md（证明独立主题不被强行归纳）
- [MEDIUM] 工作流步骤 6 与步骤 7 先后语义不明确 — 重写工作流步骤，明确归纳检查的位置和触发条件
</objective>

<execution_context>
@/Users/Bai/Downloads/同步空间/works/Code/idea-to-task/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Bai/Downloads/同步空间/works/Code/idea-to-task/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-judgment-output-refactor/01-CONTEXT.md
@.planning/phases/01-judgment-output-refactor/01-01-SUMMARY.md

<interfaces>
<!-- Plan 01 执行后 decision-rules.md 的章节结构 -->
Plan 01 产出的 decision-rules.md 章节:
  ## 1. 输入类型前置判断：是否为战略型输入
  ## 2. 主题密度判断（含修改后的条件式"禁止强行汇总"措辞）
  ## 3. 向上归纳检查（新增）
  ## 4. 可任务化程度
  ## 5. 何时补问
  ## 6. 证据强度与下钻深度
  ## 7. 高风险歧义
  ## 8. 常见基线偏差

<!-- Plan 01 产出的 output-template.md 关键新增内容 -->
新增内容:
  - 第 1 节末尾：归纳型核心判断子段（3-6句约束）
  - 第 2 节末尾：「单主题（归纳）」标记说明
  - 第 3 节开头：模板选择优先级（战略型 > 归纳型 > 常规）
  - 第 3 节中间：归纳型任务树模板（里程碑结构）

<!-- SKILL.md 当前工作流步骤（需修改） -->
当前工作流:
  1. 识别输入类型
  2. 前置判断：是否为战略型输入（引用 decision-rules.md 第 1 节）
  3. 判断主题密度
  4. 判断可任务化程度
  5. 将原始表达归一为稳定意图
  6. 必要时拆主题（战略型输入第一主题固定为总体方案设计）
  7. 输出

<!-- SKILL.md 当前核心原则（需修改的条目） -->
当前核心原则末条:
  "**战略型输入必须先出顶层方案**：涉及公司级变革时，任务树第一主题永远是顶层设计..."

<!-- 现有 fixture 文件格式 -->
fixture 格式:
  # 场景 N：[场景名]
  ## 输入
  [原始输入文本]
  ## 期望行为
  - [bullet list 描述期望行为]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL.md 工作流新增归纳步骤 + 核心原则泛化</name>
  <files>skills/idea-to-task/SKILL.md</files>
  <read_first>
    - skills/idea-to-task/SKILL.md（查看完整当前内容）
    - skills/idea-to-task/references/decision-rules.md（确认 Plan 01 执行后的章节编号）
    - .planning/phases/01-judgment-output-refactor/01-CONTEXT.md（D-09, D-10 决策）
  </read_first>
  <action>
对 SKILL.md 进行以下修改：

**修改 1 — 工作流步骤更新（per D-09, addresses review: 步骤先后语义不明确）**

将当前工作流 7 个步骤替换为 8 个步骤：

```
1. 识别输入类型：长文本、碎片短句或混合输入
2. **前置判断：是否为战略型输入**（多部门变革 + 需统一治理 + 跨季度），详见 [decision-rules.md](./references/decision-rules.md) 第 1 节
3. 判断主题密度：单主题、多相关主题、多不相关主题
4. 判断可任务化程度：能否直接形成任务树
5. 将原始表达归一为稳定意图
6. 必要时拆主题，再分别生成任务树（战略型输入第一主题固定为总体方案设计）
7. **向上归纳检查**：对非战略型的多主题输入，检查各主题是否服务于同一更高目标，若是则收敛为单顶层主题（详见 [decision-rules.md](./references/decision-rules.md) 第 3 节）
8. 输出核心判断、主题拆分、任务树、执行建议、风险提醒、下一步动作
```

**修改 2 — 核心原则泛化（per D-10）**

将核心原则最后一条：
```
- **战略型输入必须先出顶层方案**：涉及公司级变革时，任务树第一主题永远是顶层设计（目标定义、边界划定、治理规范、推进路线图），即使输入中没有明确提到，也要主动补入
```

替换为：
```
- **多主题输入先尝试向上归纳**：拆出多个主题后，先检查是否服务于同一更高目标——如果是，收敛为单顶层主题再展开子任务线；如果确实独立，按多主题分别输出。战略型输入作为特例，第一主题固定为顶层方案设计
```

**修改 3 — 「何时读取参考文件」段落更新引用**

在"当你需要判断……"的 bullet 中，将引用 decision-rules.md 的描述从当前文字更新为：
```
- 当你需要判断"该直接输出还是该补问"，或需要执行向上归纳检查时，读取 [decision-rules.md](./references/decision-rules.md)
```
  </action>
  <verify>
    <automated>grep -n "向上归纳" skills/idea-to-task/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "向上归纳检查" skills/idea-to-task/SKILL.md` 返回至少 2（工作流步骤 + 读取参考文件段落）
    - `grep "多主题输入先尝试向上归纳" skills/idea-to-task/SKILL.md` 有输出（核心原则泛化后的文本）
    - `grep "战略型输入作为特例" skills/idea-to-task/SKILL.md` 有输出（战略型作为归纳的特例保留）
    - `grep "decision-rules.md.*第 3 节" skills/idea-to-task/SKILL.md` 有输出（引用正确章节编号）
    - `grep -c "^[0-9]\." skills/idea-to-task/SKILL.md` 在工作流段落中返回 8（8 个步骤）
    - SKILL.md 中不再包含"战略型输入必须先出顶层方案"这一原有措辞（已被替换）
  </acceptance_criteria>
  <done>SKILL.md 工作流有 8 个步骤，第 7 步为向上归纳检查且引用 decision-rules.md 第 3 节；核心原则末条已泛化为多主题通用归纳原则，战略型作为特例保留</done>
</task>

<task type="auto">
  <name>Task 2: 新增行为级验证 fixture（归纳成功 + 归纳失败）</name>
  <files>tests/fixtures/idea-to-task/04-induction-success.md, tests/fixtures/idea-to-task/05-induction-failure.md</files>
  <read_first>
    - tests/fixtures/idea-to-task/02-mixed-topics.md（参考现有 fixture 格式和措辞风格）
    - tests/fixtures/idea-to-task/01-clear-direction.md（参考期望行为的写法粒度）
    - skills/idea-to-task/references/output-template.md（确认归纳型模板的结构要求）
    - .planning/phases/01-judgment-output-refactor/01-CONTEXT.md（D-04/D-05/D-06/D-07 决策）
  </read_first>
  <action>
**创建 fixture 04 — 归纳成功场景（addresses review concern: HIGH 缺少行为级验证）**

文件：`tests/fixtures/idea-to-task/04-induction-success.md`

内容：

```markdown
# 场景 4：多主题可归纳为单顶层意图

## 输入

CEO 说：我们的新产品 X 下个季度要上线。技术那边得把核心功能跑通，运营要准备好用户增长方案，市场部配合做一波发布前的预热传播。三条线各自推，但别脱节。

## 期望行为

- 核心判断第一句必须是顶层意图，类似「CEO 真正在推动的是：产品 X 下季度上线」
- 核心判断必须说明原始三个方向（技术、运营、市场）均服务于产品上线这一目标
- 主题拆分标记为「单主题（归纳）」，不是三个并列主题
- 任务树只有一个顶层主题（产品 X 上线），技术、运营、市场降级为里程碑
- 不应出现三个并列的「主题 I / 主题 II / 主题 III」
- 不触发战略型模板（这不是公司级多部门变革，而是单产品上线项目）
```

**创建 fixture 05 — 归纳失败场景（证明不强行归纳）**

文件：`tests/fixtures/idea-to-task/05-induction-failure.md`

内容：

```markdown
# 场景 5：多主题无法归纳，独立输出

## 输入

CEO 说：产品以后肯定要 AI 化，这条线得盯着。还有，我们最近会议太多、效率太低，内部协作得提效。另一个是商业化，不能一直只讲愿景，看看能不能先找几个愿意付费的试点客户。别三个一起硬上，但你们先帮我理一理。

## 期望行为

- 拆出至少三个独立主题（AI 化、内部协作提效、商业化）
- 不应把三个主题强行归纳为一个顶层意图
- 主题拆分不应标记为「单主题（归纳）」
- 各主题应独立输出任务树，不共享一个顶层主题
- 不向读者暴露「尝试归纳但失败」的过程
- 不触发战略型模板
```
  </action>
  <verify>
    <automated>ls -la tests/fixtures/idea-to-task/04-induction-success.md tests/fixtures/idea-to-task/05-induction-failure.md && grep -c "期望行为" tests/fixtures/idea-to-task/04-induction-success.md tests/fixtures/idea-to-task/05-induction-failure.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/fixtures/idea-to-task/04-induction-success.md` 返回 0（文件存在）
    - `test -f tests/fixtures/idea-to-task/05-induction-failure.md` 返回 0（文件存在）
    - `grep -c "单主题（归纳）" tests/fixtures/idea-to-task/04-induction-success.md` 返回至少 1（归纳成功场景要求此标记）
    - `grep -c "不应.*强行归纳" tests/fixtures/idea-to-task/05-induction-failure.md` 返回至少 1（归纳失败场景验证不强行归纳）
    - `grep "顶层意图" tests/fixtures/idea-to-task/04-induction-success.md` 有输出（验证归纳后的输出结构）
    - `grep "不向读者暴露" tests/fixtures/idea-to-task/05-induction-failure.md` 有输出（验证静默回退）
    - `grep "## 输入" tests/fixtures/idea-to-task/04-induction-success.md` 有输出（格式正确）
    - `grep "## 期望行为" tests/fixtures/idea-to-task/05-induction-failure.md` 有输出（格式正确）
  </acceptance_criteria>
  <done>tests/fixtures/ 中存在 04-induction-success.md 和 05-induction-failure.md，分别验证归纳成功时输出单顶层意图+里程碑结构、归纳失败时各主题独立输出且不暴露内部过程</done>
</task>

<task type="auto">
  <name>Task 3: 全局结构校验</name>
  <files></files>
  <read_first>
    - skills/idea-to-task/SKILL.md（确认修改后的完整状态）
    - skills/idea-to-task/references/decision-rules.md（确认章节编号一致性）
    - skills/idea-to-task/references/output-template.md（确认归纳型模板存在）
  </read_first>
  <action>
运行 skill 结构校验脚本，验证所有文件的引用一致性：

```bash
uv run skills/idea-to-task/scripts/validate_skill.py skills/idea-to-task --strict
```

额外手动检查点：
1. SKILL.md 工作流步骤 2 引用 "decision-rules.md 第 1 节" — 确认 decision-rules.md 第 1 节仍为「输入类型前置判断」
2. SKILL.md 工作流步骤 7 引用 "decision-rules.md 第 3 节" — 确认 decision-rules.md 第 3 节为「向上归纳检查」
3. output-template.md 中模板优先级引用 "decision-rules.md 第 1 节" 和 "decision-rules.md 第 3 节" — 确认编号正确
4. 确认 fixture 04/05 的场景编号不与现有 01/02/03 冲突
  </action>
  <verify>
    <automated>uv run skills/idea-to-task/scripts/validate_skill.py skills/idea-to-task --strict</automated>
  </verify>
  <acceptance_criteria>
    - `uv run skills/idea-to-task/scripts/validate_skill.py skills/idea-to-task --strict` 退出码为 0
    - `grep "第 1 节" skills/idea-to-task/SKILL.md` 输出包含"战略型输入"相关描述
    - `grep "第 3 节" skills/idea-to-task/SKILL.md` 输出包含"向上归纳"相关描述
  </acceptance_criteria>
  <done>validate_skill.py --strict 通过，所有文件间的章节编号引用一致，fixture 编号不冲突</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 指令消费 | SKILL.md 和 reference 文件被 LLM 读取后驱动输出行为 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | SKILL.md | accept | 纯 prompt 文件，git 版本控制保护 |
| T-01-02 | I (Information Disclosure) | fixture 文件 | accept | 测试数据不含敏感信息 |
</threat_model>

<verification>
1. `uv run skills/idea-to-task/scripts/validate_skill.py skills/idea-to-task --strict` 退出码 0
2. `grep -c "向上归纳" skills/idea-to-task/SKILL.md` >= 2
3. `ls tests/fixtures/idea-to-task/ | wc -l` 返回 5（原 3 + 新 2）
4. `grep "第 3 节" skills/idea-to-task/SKILL.md` 有输出且与 decision-rules.md 第 3 节对应
</verification>

<success_criteria>
- SKILL.md 工作流 8 步，含独立的向上归纳检查步骤
- SKILL.md 核心原则已泛化为多主题通用归纳原则
- 新增 2 个行为级 fixture 覆盖归纳成功和归纳失败场景
- validate_skill.py --strict 通过
- 所有跨文件章节编号引用一致
</success_criteria>

<output>
After completion, create `.planning/phases/01-judgment-output-refactor/01-02-SUMMARY.md`
</output>
