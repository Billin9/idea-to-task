---
phase: 01-judgment-output-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - skills/idea-to-task/SKILL.md
autonomous: true
requirements:
  - JUDGE-01
  - JUDGE-02
  - OUT-01
  - OUT-02

must_haves:
  truths:
    - "SKILL.md 工作流在拆主题和输出之间存在独立的「向上归纳检查」步骤"
    - "SKILL.md 核心原则包含通用的多主题向上归纳规则，战略型作为特例保留"
    - "skill 验证脚本通过，结构完整性不被破坏"
  artifacts:
    - path: "skills/idea-to-task/SKILL.md"
      provides: "工作流新步骤 + 核心原则更新"
      contains: "向上归纳检查"
  key_links:
    - from: "SKILL.md 工作流步骤（向上归纳检查）"
      to: "decision-rules.md 第 3 节"
      via: "工作流引用 decision-rules.md"
      pattern: "decision-rules.md"
    - from: "SKILL.md 工作流步骤（向上归纳检查）"
      to: "output-template.md 归纳型模板"
      via: "归纳成功后走归纳型模板"
      pattern: "output-template.md"
---

<objective>
更新 SKILL.md 的工作流和核心原则，将 Plan 01 中定义的归纳逻辑和归纳型模板接入 skill 入口文件，并验证整体结构完整性。

Purpose: 让 skill 入口文件反映新增的归纳能力，使模型在执行 skill 时能走到归纳路径
Output: SKILL.md 工作流和原则更新完成，验证通过
</objective>

<execution_context>
@/Users/Bai/Downloads/同步空间/works/Code/idea-to-task/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Bai/Downloads/同步空间/works/Code/idea-to-task/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-judgment-output-refactor/01-CONTEXT.md
@.planning/phases/01-judgment-output-refactor/01-01-SUMMARY.md

@skills/idea-to-task/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 SKILL.md 工作流和核心原则</name>
  <files>skills/idea-to-task/SKILL.md</files>
  <read_first>
    - skills/idea-to-task/SKILL.md (当前完整内容，理解工作流步骤编号和核心原则列表)
    - .planning/phases/01-judgment-output-refactor/01-CONTEXT.md (D-09, D-10 的决策定义)
    - skills/idea-to-task/references/decision-rules.md (确认归纳步骤已写入第 3 节)
  </read_first>
  <action>
修改 SKILL.md 的两个部分：

**1. 核心原则部分（per D-10）：**

将现有的最后一条原则：
```
- **战略型输入必须先出顶层方案**：涉及公司级变革时，任务树第一主题永远是顶层设计（目标定义、边界划定、治理规范、推进路线图），即使输入中没有明确提到，也要主动补入
```

替换为更通用的归纳原则，战略型作为特例保留：
```
- **多主题输入先尝试向上归纳**：拆出多个主题后，先检查是否存在目标从属关系，如果多个主题服务于同一个更高层目标则收敛为单顶层意图再展开。战略型输入（公司级变革）是此规则的典型场景，其第一主题固定为顶层设计方案
```

**2. 工作流部分（per D-09）：**

在现有步骤 6（拆主题）和步骤 7（输出）之间插入新步骤。现有步骤编号需要调整。

将工作流编号步骤从：
```
1. 识别输入类型：长文本、碎片短句或混合输入
2. **前置判断：是否为战略型输入**（多部门变革 + 需统一治理 + 跨季度），详见 [decision-rules.md](./references/decision-rules.md) 第 1 节
3. 判断主题密度：单主题、多相关主题、多不相关主题
4. 判断可任务化程度：能否直接形成任务树
5. 将原始表达归一为稳定意图
6. 必要时拆主题，再分别生成任务树（战略型输入第一主题固定为总体方案设计）
7. 输出核心判断、主题拆分、任务树、执行建议、风险提醒、下一步动作
```

改为：
```
1. 识别输入类型：长文本、碎片短句或混合输入
2. **前置判断：是否为战略型输入**（多部门变革 + 需统一治理 + 跨季度），详见 [decision-rules.md](./references/decision-rules.md) 第 1 节
3. 判断主题密度：单主题、多相关主题、多不相关主题
4. 判断可任务化程度：能否直接形成任务树
5. 将原始表达归一为稳定意图
6. 必要时拆主题，再分别生成任务树（战略型输入第一主题固定为总体方案设计）
7. **向上归纳检查**：拆出多主题时，检测是否存在目标从属关系，存在则收敛为单顶层主题走归纳型输出，详见 [decision-rules.md](./references/decision-rules.md) 第 3 节
8. 输出核心判断、主题拆分、任务树、执行建议、风险提醒、下一步动作
```

注意：不修改工作流下方的「遇到边界不清的输入时」部分，保持不变。
  </action>
  <verify>
    <automated>grep -n "向上归纳检查" skills/idea-to-task/SKILL.md && grep -n "多主题输入先尝试向上归纳" skills/idea-to-task/SKILL.md && grep -c "^[0-9]\." skills/idea-to-task/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md 工作流包含编号步骤 `7. **向上归纳检查**`
    - 步骤 7 包含文字 `目标从属关系`
    - 步骤 7 包含链接 `[decision-rules.md](./references/decision-rules.md) 第 3 节`
    - 原步骤 7（输出）变为步骤 8，文字为 `输出核心判断、主题拆分、任务树、执行建议、风险提醒、下一步动作`
    - 核心原则部分包含 `多主题输入先尝试向上归纳`
    - 核心原则部分包含 `战略型输入（公司级变革）是此规则的典型场景`
    - 原有的「战略型输入必须先出顶层方案」整条被替换，不再出现
    - 工作流共 8 个编号步骤
  </acceptance_criteria>
  <done>SKILL.md 工作流包含向上归纳检查步骤（第 7 步），核心原则已泛化为多主题归纳规则</done>
</task>

<task type="auto">
  <name>Task 2: 运行 skill 验证脚本确认结构完整性</name>
  <files>skills/idea-to-task/SKILL.md</files>
  <read_first>
    - skills/idea-to-task/scripts/validate_skill.py (理解验证脚本检查哪些内容)
    - skills/idea-to-task/SKILL.md (确认 Task 1 修改结果)
  </read_first>
  <action>
运行 skill 验证脚本，确认修改后的 skill 结构仍然完整：

```bash
uv run skills/idea-to-task/scripts/validate_skill.py skills/idea-to-task --strict
```

如果验证失败，根据错误信息修复 SKILL.md 或引用文件中的问题。常见可能的问题：
- SKILL.md frontmatter 格式错误 → 检查 YAML 格式
- 必需的 section 缺失 → 确认「适用场景」「核心原则」「工作流」「补问规则」「输出结构」都存在
- 引用文件不存在 → 确认 references/ 目录下三个文件路径正确
- openai.yaml 格式错误 → 检查 agents/openai.yaml（--strict 模式会检查）

验证通过后，手动检查以下关键点：
1. grep 确认 decision-rules.md 中 `## 3. 向上归纳检查` 存在
2. grep 确认 output-template.md 中 `归纳型` 至少出现 5 次
3. grep 确认 SKILL.md 中 `向上归纳检查` 存在
  </action>
  <verify>
    <automated>uv run skills/idea-to-task/scripts/validate_skill.py skills/idea-to-task --strict 2>&1 && echo "VALIDATION_PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run skills/idea-to-task/scripts/validate_skill.py skills/idea-to-task --strict` 退出码为 0
    - 输出中不包含 `ERROR` 或 `FAIL`
    - 输出包含验证通过的确认信息
  </acceptance_criteria>
  <done>skill 验证脚本 --strict 模式通过，三个修改文件的结构完整性得到确认</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

本 plan 修改的是 AI prompt 文件（Markdown），不涉及代码执行、用户输入、网络通信或数据存储。无信任边界。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | N/A | Markdown prompt file | accept | 纯 prompt 文件修改，无安全攻击面 |
</threat_model>

<verification>
1. SKILL.md 工作流包含 8 个编号步骤，第 7 步为向上归纳检查
2. SKILL.md 核心原则包含泛化后的归纳规则
3. `uv run skills/idea-to-task/scripts/validate_skill.py skills/idea-to-task --strict` 通过
</verification>

<success_criteria>
- SKILL.md 入口文件正确引用 decision-rules.md 第 3 节和归纳型模板
- 核心原则从战略型专用扩展为通用归纳规则
- skill 验证脚本在 --strict 模式下通过
</success_criteria>

<output>
After completion, create `.planning/phases/01-judgment-output-refactor/01-02-SUMMARY.md`
</output>
