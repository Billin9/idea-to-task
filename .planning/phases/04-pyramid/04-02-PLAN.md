---
phase: 04-pyramid
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - skills/idea-to-task/SKILL.md
  - skills/idea-to-task/references/decision-rules.md
autonomous: true
requirements: [PYMD-01, PYMD-02]
must_haves:
  truths:
    - "SKILL.md 步骤 5「主题识别与组织」不改标题，仅在现有句末补充内部约束：每个主题同层 2 到 4 个，向上支撑同一个核心判断"
    - "SKILL.md 末尾「明确禁止」段新增一条：不得在用户可见输出中出现「支柱」「金字塔」「pillar」「MECE」「So-what」字串（原有方法论术语禁令保留并扩展）"
    - "decision-rules.md §3「证据强度与 SCQA 完整度」的临时深度接口文字（「弱证据：只输出项目级 / 中等证据：可输出到里程碑级 / 强证据：才允许输出执行项级」）替换为金字塔弹性层级术语（弱=2 层：核心判断+主题 / 中=3 层：+里程碑 / 强=4 层：+执行项，允许 ±1 弹性）"
    - "decision-rules.md §3 中原先的「具体展开层级会在 Phase 4 用金字塔弹性结构替代」过渡说明已删除（该接口已兑现）"
    - "decision-rules.md §1「特殊输入类型处理」末尾新增一句指向 output-template.md §3 战略型条件分支的引用，保证规则与模板相互可达"
    - "SKILL.md / decision-rules.md / output-template.md 三处关于「同层 2 到 4 个」「弱=2 层 / 中=3 层 / 强=4 层 ±1」的表述字面一致（Phase 3 REVIEWS Takeaway 1）"
  artifacts:
    - path: "skills/idea-to-task/SKILL.md"
      provides: "步骤 5 金字塔支撑约束与术语泄漏扩展禁令"
      contains: "2 到 4"
    - path: "skills/idea-to-task/references/decision-rules.md"
      provides: "§3 金字塔弹性层级表述"
      contains: "弹性"
  key_links:
    - from: "SKILL.md 步骤 5"
      to: "output-template.md §3 基础骨架"
      via: "「详见 output-template.md」引用"
      pattern: "output-template\\.md"
    - from: "decision-rules.md §3"
      to: "output-template.md §3 层级规则"
      via: "术语一致：弱=2 层 / 中=3 层 / 强=4 层 ±1"
      pattern: "弱.*2 层|中.*3 层|强.*4 层"
    - from: "decision-rules.md §1 特殊输入类型处理"
      to: "output-template.md §3 战略型触发条件分支"
      via: "相互引用"
      pattern: "output-template\\.md"
---

<objective>
把 Plan 04-01 新写的金字塔模板的「内部行为约束」同步落到 SKILL.md 步骤 5 和 decision-rules.md §3 / §1 衔接段，使三份文件（SKILL.md / decision-rules.md / output-template.md）关于层级数量、弹性深度、术语禁令三件事字面一致，消除 Phase 3 评审指出的「跨文件契约漂移」风险。

Purpose: 规则三处一致是 Phase 3 REVIEWS 明确要求的纪律。Plan 04-01 建骨架，本 plan 把配套的内部推演规则（主题=金字塔支撑点的约束、证据强度→层级深度的新表述、术语扩展禁令）对齐到同一字面。
Output: 最小改动的 SKILL.md（仅步骤 5、明确禁止段）与 decision-rules.md（仅 §3，以及 §1 末尾新增一句引用）。
</objective>

<execution_context>
@/Users/Bai/Downloads/同步空间/works/Code/idea-to-task/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Bai/Downloads/同步空间/works/Code/idea-to-task/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-pyramid/04-CONTEXT.md
@.planning/phases/04-pyramid/04-01-PLAN.md
@.planning/phases/03-scqa/03-REVIEWS.md
@skills/idea-to-task/SKILL.md
@skills/idea-to-task/references/decision-rules.md
@skills/idea-to-task/references/output-template.md

<interfaces>
<!-- 本 plan 依赖 Plan 04-01 已完成的 output-template.md §3 新骨架中的字面约定 -->

来自 Plan 04-01 新写的 output-template.md §3（本 plan 必须与之对齐的字面）：
- 同层支撑点数量：「2 到 4 个」
- 弹性层级：「弱证据：展开到主题层（2 层）」「中等证据：展开到里程碑层（3 层）」「强证据：展开到执行项层（4 层）」「±1 层弹性」
- 条件触发类别：战略型 / 归纳型 / 常规 三分支，判定在 decision-rules.md 第 1 节

来自 SKILL.md 现状（Phase 3 完成态，本 plan 不改标题不改步骤编号）：
- 步骤 5 全文：「主题识别与组织：基于 SCQA 推演结果识别主题结构；当多个主题服务于同一核心判断时，收敛为单顶层主题，确实独立时分别处理」
- 「明确禁止」段末两条已包含「不在输出中暴露 SCQA、Situation、Complication、Question 等方法论术语」——本 plan 在此扩展，不替换

来自 decision-rules.md §3 现状（Phase 3 临时深度接口，本 plan 要替换的片段）：
```
当前阶段只建立"完整度 → 证据强度"的映射，具体展开层级会在 Phase 4 用金字塔弹性结构替代。为了兼容当前模板，暂时保留以下深度接口：

- 弱证据：只输出项目级
- 中等证据：可输出到里程碑级
- 强证据：才允许输出执行项级
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL.md 步骤 5 补充金字塔支撑约束 + 扩展术语泄漏禁令</name>
  <files>skills/idea-to-task/SKILL.md</files>
  <read_first>
    - skills/idea-to-task/SKILL.md（完整 80 行）
    - skills/idea-to-task/references/output-template.md（完成 Plan 04-01 后的新 §3，确认字面「2 到 4」与弹性层级表述）
    - .planning/phases/04-pyramid/04-CONTEXT.md（D-05、D-06、D-11 硬约束：不改步骤标题、主题术语对用户透明）
    - .planning/phases/03-scqa/03-REVIEWS.md Takeaway 三条（规则三处一致、大任务拆小、验收可 grep）
  </read_first>
  <action>
仅做两处微调，其余字节不动：

**改动 A — 步骤 5「主题识别与组织」补充金字塔支撑约束**

现状（找到这一整行）：
```
5. 主题识别与组织：基于 SCQA 推演结果识别主题结构；当多个主题服务于同一核心判断时，收敛为单顶层主题，确实独立时分别处理
```

替换为（保持「5.」编号、「主题识别与组织」标题不变，仅在同一行后扩展句子，不新增小标题、不拆成多行）：
```
5. 主题识别与组织：基于 SCQA 推演结果识别主题结构；当多个主题服务于同一核心判断时，收敛为单顶层主题，确实独立时分别处理；每个主题作为同一核心判断下的分层支撑点，同层主题数量保持在 2 到 4 个，不在输出中使用「支柱」等内部术语，详见 [output-template.md](./references/output-template.md) 第 3 节
```

**改动 B —「明确禁止」段扩展术语禁令**

现状（找到这一行）：
```
- 不在输出中暴露 SCQA、Situation、Complication、Question 等方法论术语
```

替换为（保持条目项列样式，扩展为两项；其它禁令不动）：
```
- 不在输出中暴露 SCQA、Situation、Complication、Question、Answer 等方法论术语
- 不在输出中使用「支柱」「金字塔」「pillar」「MECE」「So-what」等内部方法论术语
```

实施顺序约束：
1. 用 Read 读取 SKILL.md 全文，定位步骤 5 与「明确禁止」段的精确字面。
2. 用 Edit 工具做精准字面替换，一次一处，避免 regex 误伤。
3. 不得改动除上述两处以外的任何行（包括空行、标点、markdown 语法）。
4. 不得改动步骤 5 的编号与「主题识别与组织」这五个字；不得增设步骤 6 / 7。

禁止项：
- 不得引入「金字塔」「支柱」「pillar」等术语到步骤正文（这些词只能出现在「明确禁止」段作为禁令本身）。
- 不得把 output-template.md §3 的战略型 / 归纳型 / 常规分类复写到 SKILL.md。
- 不得改动其它工作流步骤（1、2、3、4、6 原文保留）。
  </action>
  <verify>
    <automated>bash -c 'cd /Users/Bai/Downloads/同步空间/works/Code/idea-to-task && \
      # 改动 A 断言：步骤 5 包含「2 到 4 个」和 output-template.md 引用
      grep -q "5\. 主题识别与组织" skills/idea-to-task/SKILL.md && \
      grep -q "同层主题数量保持在 2 到 4 个" skills/idea-to-task/SKILL.md && \
      grep -q "output-template.md" skills/idea-to-task/SKILL.md && \
      # 改动 B 断言：术语禁令已扩展
      grep -q "不在输出中暴露 SCQA、Situation、Complication、Question、Answer 等方法论术语" skills/idea-to-task/SKILL.md && \
      grep -q "不在输出中使用「支柱」「金字塔」「pillar」「MECE」「So-what」等内部方法论术语" skills/idea-to-task/SKILL.md && \
      # 反向断言：SKILL.md 主流程正文不得把「金字塔/支柱」当正面术语使用（只允许作为禁令出现）
      # 即步骤 1-6 正文行里不得单独出现「金字塔」「支柱」字串
      ! awk "/^## 工作流/,/^## 补问规则/" skills/idea-to-task/SKILL.md | grep -qE "金字塔|支柱|pillar" && \
      # validator 通过
      uv run skills/idea-to-task/scripts/validate_skill.py skills/idea-to-task --strict && \
      echo "TASK1_ASSERTIONS_PASSED"'</automated>
  </verify>
  <acceptance_criteria>
    - 步骤 5 标题行仍以「5. 主题识别与组织：」开头，未被改名
    - `grep "同层主题数量保持在 2 到 4 个" SKILL.md` 至少 1 行
    - `grep "output-template.md" SKILL.md` 至少出现在步骤 5 一次
    - `grep "不在输出中使用「支柱」「金字塔」「pillar」「MECE」「So-what」等内部方法论术语" SKILL.md` 返回 1 行
    - 工作流段（`## 工作流` 到 `## 补问规则` 之间）不得出现「金字塔」「支柱」「pillar」字串（只允许在禁令段出现）
    - `uv run validate_skill.py skills/idea-to-task --strict` 退出码 0
  </acceptance_criteria>
  <done>SKILL.md 两处微调完成，步骤 5 写入金字塔支撑约束且对用户术语透明，术语禁令扩展覆盖支柱/金字塔/MECE/So-what，validator 通过。</done>
</task>

<task type="auto">
  <name>Task 2: decision-rules.md §3 层级表述替换 + §1 末尾衔接引用</name>
  <files>skills/idea-to-task/references/decision-rules.md</files>
  <read_first>
    - skills/idea-to-task/references/decision-rules.md（完整 106 行，定位 §1 末尾「特殊输入类型处理」和 §3 末尾「深度接口」文字）
    - skills/idea-to-task/references/output-template.md（完成 Plan 04-01 后的新 §3，确认术语字面）
    - skills/idea-to-task/SKILL.md（完成本 plan Task 1 后的新步骤 5，确认术语字面）
    - .planning/phases/04-pyramid/04-CONTEXT.md（D-04、D-07、D-08 硬约束：§1 条件规则保留不下移、§3 替换为金字塔术语、极弱证据不强造第三层）
  </read_first>
  <action>
仅做两处改动，其余字节不动：

**改动 A — §3 末尾「深度接口」文字替换为金字塔弹性层级术语（对应 D-07）**

现状（找到从「当前阶段只建立…」开始到「强证据：才允许输出执行项级」结束的整段，含前导空行和列表）：
```
当前阶段只建立"完整度 → 证据强度"的映射，具体展开层级会在 Phase 4 用金字塔弹性结构替代。为了兼容当前模板，暂时保留以下深度接口：

- 弱证据：只输出项目级
- 中等证据：可输出到里程碑级
- 强证据：才允许输出执行项级
```

替换为：
```
证据强度与分层任务树的深度按以下弹性规则联动（详见 [output-template.md](./output-template.md) 第 3 节层级规则）：

- 弱证据：展开到主题层，即 2 层结构（核心判断 → 主题）
- 中等证据：展开到里程碑层，即 3 层结构（核心判断 → 主题 → 里程碑）
- 强证据：展开到执行项层，即 4 层结构（核心判断 → 主题 → 里程碑 → 执行项）

上述层级允许 ±1 层弹性；当输入天然只能支撑 2 层时（极弱证据），不强造第三层，停在主题层并在「风险与高风险推断」中标注不确定性。
```

**改动 B — §1「特殊输入类型处理」段末尾新增一句衔接引用（对应 D-04）**

找到 §1「特殊输入类型处理」段的最后一行：
```
- 其余主题再按"可并行的诊断线"和"必须后置的落地线"展开。
```

在其下方紧接一空行后新增一条项列（不合并到原有列表，作为段末补注，保持缩进一致）：
```

识别规则在此节为单一事实源；模板侧（output-template.md 第 3 节）按本节触发条件填充战略型条件分支，不复写触发规则。
```

实施顺序约束：
1. 用 Read 读取 decision-rules.md 全文，定位 §1 末尾与 §3 末尾两处精确字面。
2. 用 Edit 工具整段替换 §3 的「深度接口」文字（改动 A），一次完成。
3. 用 Edit 工具在 §1 末尾插入衔接引用（改动 B），不改原有文字。
4. §2、§4、§5 任何字符不得改动；§3 前半段（强/中/弱定义三条）不得改动。

禁止项：
- 不得在 §3 中复写 output-template.md §3 的条件触发规则（避免双事实源）。
- 不得删除 §1「特殊输入类型处理」的任何现有条款；只能在末尾追加一条。
- 不得把「金字塔」「支柱」写进用户可见术语——本文件是内部规则文件，这些词允许出现，但如有用户侧文案务必审慎。本 plan 的新表述中允许使用「层级」「展开到…层」「弹性」，不使用「金字塔」/「支柱」以保持与 SKILL.md 主工作流正文的一致回避策略。
  </action>
  <verify>
    <automated>bash -c 'cd /Users/Bai/Downloads/同步空间/works/Code/idea-to-task && \
      # 改动 A 断言：旧文字已删除
      ! grep -q "具体展开层级会在 Phase 4 用金字塔弹性结构替代" skills/idea-to-task/references/decision-rules.md && \
      ! grep -q "暂时保留以下深度接口" skills/idea-to-task/references/decision-rules.md && \
      ! grep -q "弱证据：只输出项目级" skills/idea-to-task/references/decision-rules.md && \
      ! grep -q "中等证据：可输出到里程碑级" skills/idea-to-task/references/decision-rules.md && \
      ! grep -q "强证据：才允许输出执行项级" skills/idea-to-task/references/decision-rules.md && \
      # 改动 A 断言：新文字出现
      grep -q "弱证据：展开到主题层" skills/idea-to-task/references/decision-rules.md && \
      grep -q "中等证据：展开到里程碑层" skills/idea-to-task/references/decision-rules.md && \
      grep -q "强证据：展开到执行项层" skills/idea-to-task/references/decision-rules.md && \
      grep -q "2 层结构" skills/idea-to-task/references/decision-rules.md && \
      grep -q "3 层结构" skills/idea-to-task/references/decision-rules.md && \
      grep -q "4 层结构" skills/idea-to-task/references/decision-rules.md && \
      grep -q "±1 层弹性" skills/idea-to-task/references/decision-rules.md && \
      grep -q "极弱证据" skills/idea-to-task/references/decision-rules.md && \
      grep -q "output-template.md" skills/idea-to-task/references/decision-rules.md && \
      # 改动 B 断言：§1 末尾新增引用
      grep -q "识别规则在此节为单一事实源" skills/idea-to-task/references/decision-rules.md && \
      # 反向断言：decision-rules.md 本文件允许出现「金字塔」（内部规则文件可以），但本次新增文字不使用
      # 即新增的两段内不出现「金字塔」「支柱」
      ! grep -qE "弱证据：展开到主题层.*金字塔" skills/idea-to-task/references/decision-rules.md && \
      # 三处一致断言：SKILL.md + decision-rules.md + output-template.md 关于「2 到 4」的字面存在
      grep -q "2 到 4" skills/idea-to-task/SKILL.md && \
      grep -q "2 到 4" skills/idea-to-task/references/output-template.md && \
      # validator 通过
      uv run skills/idea-to-task/scripts/validate_skill.py skills/idea-to-task --strict && \
      echo "TASK2_ASSERTIONS_PASSED"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep "具体展开层级会在 Phase 4 用金字塔弹性结构替代" decision-rules.md` 返回 0 行
    - `grep "弱证据：只输出项目级" decision-rules.md` 返回 0 行
    - `grep "弱证据：展开到主题层" decision-rules.md` 返回 1 行
    - `grep "中等证据：展开到里程碑层" decision-rules.md` 返回 1 行
    - `grep "强证据：展开到执行项层" decision-rules.md` 返回 1 行
    - `grep "2 层结构" decision-rules.md`、`grep "3 层结构" decision-rules.md`、`grep "4 层结构" decision-rules.md` 各至少 1 行
    - `grep "±1 层弹性" decision-rules.md` 至少 1 行
    - `grep "极弱证据" decision-rules.md` 至少 1 行
    - `grep "识别规则在此节为单一事实源" decision-rules.md` 返回 1 行
    - 三处一致：SKILL.md / decision-rules.md / output-template.md 全部包含「2 到 4」字串
    - `uv run validate_skill.py skills/idea-to-task --strict` 退出码 0
  </acceptance_criteria>
  <done>decision-rules.md §3 旧深度接口文字彻底被金字塔弹性层级描述替换；§1 末尾新增一条与 output-template 的单一事实源声明；三份文件关于同层 2-4、弹性 2/3/4 层的字面一致。</done>
</task>

</tasks>

<threat_model>
## 适用性

**N/A — 纯文档/提示工程改动，不涉及安全边界。**

**理由：**
- 本 plan 只修改 Markdown 指令文件（SKILL.md、decision-rules.md）
- 无代码执行、无数据流、无认证、无存储
- LLM runtime 以只读提示形式消费这些文件，无信任边界穿越
- 术语泄漏、跨文件契约漂移属于「输出质量护栏」而非安全风险，已通过 grep 断言在 verify 步骤覆盖
</threat_model>

<verification>
- Task 1、Task 2 的 grep 断言全部通过
- `uv run skills/idea-to-task/scripts/validate_skill.py skills/idea-to-task --strict` 退出码 0
- 跨文件一致性 spot-check：
  - SKILL.md 步骤 5 / decision-rules.md §3 / output-template.md §3 关于「同层 2 到 4 个」字面一致
  - 关于「弱=2 层 / 中=3 层 / 强=4 层 ±1」三处描述语义一致（允许 SKILL.md 不复述具体层数，但 decision-rules.md 与 output-template.md 必须字面一致）
  - decision-rules.md §1 末尾的「单一事实源」声明指向 output-template.md §3
</verification>

<success_criteria>
- SKILL.md 步骤 5 补充金字塔支撑约束（PYMD-01 内部落地）
- decision-rules.md §3 采用弹性层级术语，原临时接口文字已消除（PYMD-02）
- 三份文件字面一致，不再有 Phase 3 REVIEWS 所担心的跨文件契约漂移
- 规则单一事实源原则（D-04）通过 §1 末尾衔接句明确声明
</success_criteria>

<output>
After completion, create `.planning/phases/04-pyramid/04-02-SUMMARY.md`.
</output>
