---
phase: 02-example-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/idea-to-task/references/examples.md
autonomous: true
requirements:
  - EX-01
must_haves:
  truths:
    - "examples.md 中存在案例 7，包含同一输入的错误示例（平铺）和正确示例（归纳）两段对比"
    - "正确示例的核心判断第一句是顶层意图格式：「CEO 真正在推动的是：产品 X 下季度上线」"
    - "案例 2 的处理方式中体现了向上归纳检查步骤，但不使用「静默回退」等内部术语"
    - "案例 7 检查点包含「不触发战略型模板」边界约束"
  artifacts:
    - path: "skills/idea-to-task/references/examples.md"
      provides: "正反对比案例 + 案例 2 修复"
      contains: "案例 7"
  key_links:
    - from: "skills/idea-to-task/references/examples.md (案例 7 正确示例)"
      to: "skills/idea-to-task/references/output-template.md (归纳型模板)"
      via: "核心判断格式、主题拆分标记、任务树结构必须一致"
      pattern: "CEO 真正在推动的是"
    - from: "skills/idea-to-task/references/examples.md (案例 2)"
      to: "skills/idea-to-task/references/decision-rules.md (第 3 节)"
      via: "归纳检查流程描述必须一致，但不暴露内部术语"
      pattern: "向上归纳检查"
---

<objective>
在 examples.md 中新增一组正反对比案例（案例 7）并修复案例 2，让模型能直接参照「平铺输出（错误）」和「归纳输出（正确）」的差异来对齐期望行为。

Purpose: 补充 few-shot 示例，从「仅靠规则推理」升级为「规则 + 示例双重锚定」，降低模型输出偏差概率。
Output: 更新后的 examples.md，包含 7 个案例。
</objective>

<execution_context>
@/Users/Bai/Downloads/同步空间/works/Code/idea-to-task/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Bai/Downloads/同步空间/works/Code/idea-to-task/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-example-calibration/02-CONTEXT.md

<interfaces>
<!-- 执行器需要对照的格式规范，从 output-template.md 和 decision-rules.md 提取 -->

归纳型核心判断格式（output-template.md 第 1 节）：
```markdown
CEO 真正在推动的是：[顶层意图]。原始拆出的 [主题A]、[主题B]、[主题C] 均服务于这一顶层目标，归纳为单主题。
```

归纳型任务树格式（output-template.md 第 3 节）：
```markdown
### 主题：[顶层意图名称]
*由 [原主题A]、[原主题B]、[原主题C] 归纳而来。*

- **里程碑 1：[原主题A 方向]**
  - [ ] [执行项]
- **里程碑 2：[原主题B 方向]**
  - [ ] [执行项]
- **里程碑 3：[原主题C 方向]**
  - [ ] [执行项]
```

主题拆分归纳标记（output-template.md 第 2 节）：
```
标记为「单主题（归纳）」，说明是由多个方向收敛而来
```

向上归纳检查流程（decision-rules.md 第 3 节）：
```
1. 对已拆出的所有主题，问：「这些方向是否都在服务同一个更大的目标？」
2. 如果存在一个目标能覆盖全部主题 → 归纳成功 → 走归纳型模板
3. 如果主题之间确实独立 → 归纳失败 → 静默回退，按原有多主题逻辑输出
```
注意：「静默回退」是 decision-rules.md 中的内部处理术语，examples.md 中不得使用此术语，应改用面向读者的自然表述。

fixture 04 原始输入：
```
CEO 说：我们的新产品 X 下个季度要上线。技术那边得把核心功能跑通，运营要准备好用户增长方案，市场部配合做一波发布前的预热传播。三条线各自推，但别脱节。
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复案例 2 + 新增案例 7 正反对比</name>
  <files>skills/idea-to-task/references/examples.md</files>
  <read_first>
    - skills/idea-to-task/references/examples.md（当前完整内容，确认案例 2 原文和案例 6 结尾位置）
    - skills/idea-to-task/references/output-template.md（归纳型模板格式，正确示例必须逐句对齐）
    - skills/idea-to-task/references/decision-rules.md（第 3 节向上归纳检查流程，案例 2 修复依据）
    - tests/fixtures/idea-to-task/04-induction-success.md（输入素材和期望行为，案例 7 素材来源）
  </read_first>
  <action>
对 examples.md 进行两处修改：

**修改 1：修复案例 2（per D-06）**

将案例 2 第 11 行的：
```
处理方式：先拆主题，再分别给任务树，可附 1 到 3 个关键问题。
```

替换为（addresses review concern: 案例 2 "静默回退"措辞风险）：
```
处理方式：先拆主题，再做向上归纳检查；因各方向不服务同一目标，按多主题分别输出，可附 1 到 3 个关键问题。
```

关键要点：
- 不使用「静默回退」——这是 decision-rules.md 的内部术语，不应出现在 examples.md 这类强提示材料中（Gemini + Codex 共识）
- 不使用「无法」「失败」等负面措辞
- 采用 Codex 建议的措辞方案：自然地表达「检查了 → 不服务同一目标 → 按多主题输出」
- 除处理方式这一句外，案例 2 其他内容不做任何修改

**修改 2：新增案例 7（per D-01 ~ D-05）**

在案例 6 之后追加以下完整内容：

```markdown
## 案例 7：归纳型输入——正反对比

输入："我们的新产品 X 下个季度要上线。技术那边得把核心功能跑通，运营要准备好用户增长方案，市场部配合做一波发布前的预热传播。三条线各自推，但别脱节。"

### ❌ 错误示例（平铺）

**核心判断：** CEO 提出了三条并行工作线：技术核心功能开发、运营用户增长方案、市场发布前预热传播。三条线各自独立推进，需注意协调。

**主题拆分：** 三个主题——技术实现、运营策略、市场传播。

**任务树摘要：**

### 主题 I：技术核心功能开发
- **里程碑：** 核心功能跑通验收

### 主题 II：运营用户增长
- **里程碑：** 用户增长方案定稿

### 主题 III：市场预热传播
- **里程碑：** 发布前传播方案落地

### ✅ 正确示例（归纳）

**核心判断：** CEO 真正在推动的是：产品 X 下季度上线。原始拆出的技术实现、运营策略、市场传播三条线均服务于这一顶层目标，归纳为单主题。当前阶段重心是确保三条线围绕同一上线节点对齐节奏，不脱节。

**主题拆分：** 单主题（归纳）

**任务树摘要：**

### 主题：产品 X 上线
*由技术实现、运营策略、市场传播归纳而来。*

- **里程碑 1：技术核心功能开发** — 核心功能跑通验收
- **里程碑 2：运营用户增长** — 用户增长方案定稿并启动
- **里程碑 3：市场预热传播** — 发布前传播节奏落地

检查点：
- 核心判断第一句必须是「CEO 真正在推动的是：产品 X 下季度上线」，不能以三条线并列开头
- 主题拆分必须标记为「单主题（归纳）」，不能拆成三个并列主题
- 任务树只有一个顶层主题，技术、运营、市场降级为里程碑，不能出现「主题 I / 主题 II / 主题 III」
- 不应出现「总体方案设计」作为独立主题——归纳型与战略型是不同路径
- 错误示例和正确示例的信息量相当，区别在于结构（平铺 vs 归纳），不是信息缺失
```

关键要点：
- 只新增这一组对比案例（per D-01）
- 输入文本直接复用 fixture 04 原文（per D-02）
- 先 ❌ 后 ✅ 的分段对比格式（per D-03）
- 展示核心判断 + 主题拆分 + 任务树摘要三个节点，任务树只到里程碑级（per D-04）
- 末尾附检查点列表（per D-05）
- 检查点第 4 条新增「不应出现"总体方案设计"作为独立主题」边界约束（addresses review concern: 检查点缺少"不触发战略型模板"边界）
- 案例 7 整体信息密度与案例 5/6 相当——错误示例和正确示例都用简短的结构化摘要，不展开到执行项 checkbox（addresses review concern: 案例 7 信息密度控制）
- 正确示例核心判断第一句严格遵循 output-template.md 归纳型格式
- 错误示例是「看起来合理但缺少归纳步骤」的平铺输出，不是胡说八道
  </action>
  <verify>
    <automated>grep -c "^## 案例" skills/idea-to-task/references/examples.md && grep -c "❌ 错误示例（平铺）" skills/idea-to-task/references/examples.md && grep -c "✅ 正确示例（归纳）" skills/idea-to-task/references/examples.md && grep -c "CEO 真正在推动的是：产品 X 下季度上线" skills/idea-to-task/references/examples.md && grep -c "单主题（归纳）" skills/idea-to-task/references/examples.md && grep -c "不应出现.*总体方案设计.*作为独立主题" skills/idea-to-task/references/examples.md && grep -c "向上归纳检查" skills/idea-to-task/references/examples.md && ! grep "静默回退" skills/idea-to-task/references/examples.md && echo "NO_SILENT_FALLBACK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## 案例" examples.md` 返回 7
    - 案例 7 包含 `### ❌ 错误示例（平铺）` 和 `### ✅ 正确示例（归纳）` 两个子标题
    - 案例 7 输入文本与 fixture 04 完全一致：包含「我们的新产品 X 下个季度要上线」
    - 正确示例核心判断第一句为「CEO 真正在推动的是：产品 X 下季度上线」
    - 正确示例主题拆分标记为「单主题（归纳）」
    - 正确示例任务树格式为「### 主题：产品 X 上线」+ 三个里程碑，无执行项 checkbox
    - 错误示例任务树格式为三个并列「### 主题 I/II/III」，无执行项 checkbox
    - 检查点包含「不应出现"总体方案设计"作为独立主题」（战略型边界约束）
    - 检查点共 5 条
    - 案例 2 的处理方式包含「向上归纳检查」字样
    - 案例 2 的处理方式包含「因各方向不服务同一目标，按多主题分别输出」
    - `grep "静默回退" examples.md` 返回 0 行匹配（整个文件不含此术语）
    - 案例 1, 3, 4, 5, 6 的内容未被修改
  </acceptance_criteria>
  <done>案例 2 修复完成（使用面向读者的自然表述替代内部术语），案例 7 完整写入（含战略型边界检查点），格式与 output-template.md 一致，信息密度与现有案例风格协调</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

无信任边界——本阶段为纯 Markdown 内容编辑，不涉及代码执行、用户输入处理或外部服务调用。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (empty) | — | — | — | 纯内容编辑阶段，无安全威胁 |
</threat_model>

<verification>
1. examples.md 包含 7 个案例（grep -c "^## 案例" 返回 7）
2. 案例 7 包含错误示例和正确示例的完整对比
3. 正确示例格式与 output-template.md 归纳型模板一致
4. 案例 7 检查点包含战略型边界约束（「不应出现"总体方案设计"」）
5. 案例 2 处理方式体现归纳检查步骤，但不使用「静默回退」术语
6. 整个 examples.md 不包含「静默回退」字样
7. 其他案例（1, 3, 4, 5, 6）内容未被修改
</verification>

<success_criteria>
- examples.md 包含 7 个案例标题
- 案例 7 的正确示例核心判断以「CEO 真正在推动的是：产品 X 下季度上线」开头
- 案例 7 的正确示例主题拆分标记为「单主题（归纳）」
- 案例 7 检查点包含「不应出现"总体方案设计"作为独立主题」
- 案例 2 的处理方式包含「向上归纳检查」，不包含「静默回退」
- examples.md 全文不包含「静默回退」
- EX-01 需求完整满足：同一输入分别展示平铺输出和归纳输出
</success_criteria>

<output>
After completion, create `.planning/phases/02-example-calibration/02-01-SUMMARY.md`
</output>
